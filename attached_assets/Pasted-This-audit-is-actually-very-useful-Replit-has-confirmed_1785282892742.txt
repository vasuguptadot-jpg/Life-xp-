This audit is actually very useful. Replit has confirmed that the project is not a complete LifeXP application yet—it is currently a functional backend foundation with authentication, onboarding, progression, and quests.

The next move should not be to build all remaining features immediately. First, fix the security and integrity issues that Replit identified.

My recommended next prompt to Replit

Give Replit this short prompt:

LIFEXP — FIX ALL CRITICAL BACKEND ISSUES FROM AUDIT

The complete functionality audit has been completed. Now fix the verified issues below.

Do not rebuild working systems unnecessarily. Do not create duplicate business logic. Make actual code changes and test them.

1. SECURITY: LOCK DOWN XP

The client must not be able to award arbitrary XP.

Remove or restrict the direct client-controlled XP award flow.

XP must be awarded only through verified server-side events, such as:

- Quest completion.
- Verified future activities.
- Other server-authorized progression events.

A user must not be able to send arbitrary XP such as:

{ "sourceType": "cheat", "xp": 99999 }

and increase their own progression.

If an internal progression service is needed, separate it from public client APIs.

Ensure XP rewards are idempotent.

2. FIX GLOBAL ERROR HANDLING

Add a global Express error handler.

Requirements:

- Catch unhandled route errors.
- Return structured JSON 500 responses.
- Do not expose raw database/PostgreSQL errors.
- Log server-side error details safely.
- Ensure requests do not hang on unexpected errors.

3. FIX QUEST XP REWARDS

When a quest is successfully completed:

- Verify ownership atomically.
- Prevent duplicate completion rewards.
- Award the correct XP through the authoritative server-side progression service.
- Ensure retries cannot award XP twice.
- Use transactions where necessary.

The gameplay loop must be:

QUEST → VERIFIED COMPLETION → XP REWARD → PROGRESSION UPDATE

4. FIX ATTRIBUTE IDEMPOTENCY

The deduplication key must be based on:

(sourceId, attribute)

A source awarding STRENGTH must not prevent ENDURANCE from being awarded for the same source.

Fix the query and add a database constraint if appropriate.

5. SEED ARCHETYPES

Add the intended archetype seed data.

Ensure:

- Seeds are repeatable/idempotent.
- Duplicate archetypes are not created.
- Onboarding can complete end-to-end using real seeded data.

6. IMPLEMENT SECURE SESSION MANAGEMENT

Implement:

- Refresh token persistence.
- Refresh token validation.
- Refresh token rotation where appropriate.
- Refresh token revocation.
- POST /auth/refresh.
- POST /auth/logout.

A stolen or compromised refresh token must be revocable.

Do not store raw refresh tokens if a secure hashed-token design is more appropriate.

7. ADD AUTH RATE LIMITING

Add rate limiting to:

- Signup.
- Signin.
- Refresh-token endpoints where appropriate.

Use sensible limits and return structured errors.

8. FIX CORS

Replace unrestricted CORS with a configurable allowlist.

Development and production origins must be configurable through environment variables.

Do not allow every origin by default in production.

9. FIX QUEST OWNERSHIP ATOMICITY

For quest progress and completion updates:

- Re-assert user ownership inside the UPDATE/transaction.
- Do not rely only on a previous SELECT ownership check.
- Prevent TOCTOU authorization issues.

10. ADD DATABASE INDEXES

Add appropriate indexes for frequently queried fields, especially:

- xp_transactions.user_id
- attribute_history.user_id
- user_goals.user_id

Review other high-frequency queries and add indexes where justified.

11. PREPARE SAFE ACCOUNT DELETION

Review foreign keys and implement a clean account deletion strategy.

Use appropriate ON DELETE CASCADE behavior or an explicit transactional deletion strategy.

Do not leave orphaned user data.

12. ADD VERSIONED MIGRATIONS

The current project relies on development schema push.

Create a proper versioned migration workflow suitable for production deployments.

Do not destroy existing data.

13. ADD LINTING

Configure linting and formatting for the backend.

Run linting and fix actual issues.

14. ADD TESTING FOUNDATION

Create a real test setup.

At minimum add tests for:

- Signup.
- Signin.
- Unauthorized access.
- User isolation.
- Refresh tokens.
- Logout/revocation.
- XP cannot be client-forged.
- Quest completion.
- Quest completion XP.
- Duplicate quest completion.
- XP idempotency.
- Attribute idempotency by (sourceId, attribute).
- Quest ownership.
- Rate limiting.
- Global error handling.

Do not create fake tests that only verify that files or functions exist.

15. VERIFY EVERYTHING

After implementation, run:

- Type check.
- Lint.
- Database migrations.
- All automated tests.
- Production build.

Test the actual API behavior.

REQUIRED FINAL REPORT

Report:

1. Exact files created.
2. Exact files modified.
3. Exact files removed.
4. Every issue fixed.
5. Every issue still remaining.
6. Test count:
   - Total
   - Passed
   - Failed
   - Skipped
7. Build result.
8. Typecheck result.
9. Lint result.
10. Migration result.
11. Security verification.
12. Whether a client can still forge XP.
13. Whether quest completion now correctly awards XP.
14. Whether refresh tokens can be revoked.
15. Whether onboarding can complete using seeded archetypes.
16. Any remaining blockers.

Do not claim an issue is fixed unless you actually changed and verified the code.Why this is the correct next step

The most dangerous issue is client-forgeable XP. If you build the entire frontend before fixing that, you will be building on an insecure progression system.

The correct sequence is:

Secure Backend → Test Backend → Build Frontend → Build Remaining Product Systems

After Replit completes this prompt, send me its next report. Then we can create the next focused prompt for the frontend foundation.